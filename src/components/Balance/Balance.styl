@import '../../css/base.styl'

text() {
  font-size: 24px;
  line-height: 36px;
  color: #546076;
  font-weight: bold;
}

.balance {
  width: 100%;
  height: 140px;
  text-transform: uppercase;
  text();
}

.capital {

}

.info {
  justify-content: space-between;
  display: flex;
  .tradeBalance {
    font-size: 75px;
    line-height: 1;
    color: $subColor;
    font-family: "S FU IDisplay";
    font-weight: bold;
    text-align: right;
  }

  .details {
    .item {
      display: flex;
      justify-content: space-between;
      .amount {
        color: $subColor;
      }
      .hIcon {

      }
    }
  }
}
